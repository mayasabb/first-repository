# dict_tuples_list = []
# hours_range_and_ip_tuple = valid_dct.get(valid_dct[j])
# print(hours_range_and_ip_tuple)
# hours_range_tuple = valid_dct[name]
# hours_range = hours_range_tuple[1].split("-")
# for hour in range(len(hours_range)):
#     hour_to_int = hours_range[hour].replace(":", ".")
#   HOUR = float(hour_to_int)
#   hours_range.append(HOUR)

# hours_range_tuple = valid_dct.get('Omer')
# print(hours_range_tuple)
# hours_range = hours_range_tuple[1].split("-")
# print(hours_range)
# for hour in range(len(hours_range)):
#     hour_to_int = hours_range[hour].replace(":", ".")
#     HOUR = float(hour_to_int)
#     hours_range.append(HOUR)
# print(hours_range)

# if 4.33 <= hours_range[2] or 4.33 >= hours_range[3]:
#     print(util.alert_suspicious(("Omer from", valid_dct[0][0]), "Wrong time"))


# dct_keys_list = valid_dct.keys()
# for j in range(len(dct_keys_list)):
#     if new_event_list[i][3] == dct_keys_list[j]:
#         if new_event_list[i][1] != "IP Address: " + valid_dct[j][0]:
#             print(util.alert_suspicious((dct_keys_list[j], "from", valid_dct[j][0]), "Wrong IP Address")